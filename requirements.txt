# requirements.txt (เวอร์ชันแก้ไขที่ตัดออกมากที่สุดเท่าที่ทำได้)

# FastAPI and its core dependencies
fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.1
h11==0.16.0
anyio==4.9.0
sniffio==1.3.1
python-multipart==0.0.20

# Pydantic and type hinting (required by FastAPI)
pydantic==2.11.7
pydantic_core==2.33.2
annotated-types==0.7.0
typing-inspection==0.4.1
typing_extensions==4.14.1

# Core ML/Image Processing Libraries (ESSENTIAL for your app)
torch==2.7.1
torchvision==0.22.1
ultralytics==8.3.166
ultralytics-thop==2.0.14
opencv-python==4.12.0.88
numpy==2.2.6
pillow==11.3.0

# HTTP/Request related (used by FastAPI/requests)
requests==2.32.4
certifi==2025.7.14
charset-normalizer==3.4.2
idna==3.10
urllib3==2.5.0

# Other common dependencies (often needed or very small)
filelock==3.18.0
fsspec==2025.5.1
PyYAML==6.0.2 # Ultralytics might use this for config
Jinja2==3.1.6
MarkupSafe==3.0.2 # Dependency of Jinja2
click==8.2.1
colorama==0.4.6
six==1.17.0 # Compatibility library, very small
packaging==25.0 # For package management, common dependency

# Date/Time handling (often needed by various libs, small)
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2

# -----------------------------------------------------------------------------
# แพ็คเกจที่ถูกตัดออกไป (และเหตุผล)
# -----------------------------------------------------------------------------
# # git-filter-repo==2.47.0 # เครื่องมือ Git, ไม่จำเป็นใน Production
# # matplotlib==3.10.3 # สำหรับพลอตกราฟ, ไม่ได้ใช้ใน app.py
# # contourpy==1.3.2 # Dependency ของ Matplotlib
# # cycler==0.12.1 # Dependency ของ Matplotlib
# # fonttools==4.58.5 # Dependency ของ Matplotlib
# # kiwisolver==1.4.8 # Dependency ของ Matplotlib
# # pyparsing==3.2.3 # Dependency ของ Matplotlib
# # pandas==2.3.1 # สำหรับจัดการข้อมูลตาราง, ไม่ได้ใช้ใน app.py
# # psutil==7.0.0 # สำหรับข้อมูลระบบ, ไม่จำเป็นใน Production
# # py-cpuinfo==9.0.0 # สำหรับข้อมูล CPU, ไม่จำเป็นใน Production
# # mpmath==1.3.0 # Dependency ของ SymPy
# # networkx==3.5 # สำหรับกราฟ, ไม่จำเป็นใน app.py
# # sympy==1.14.0 # สำหรับคณิตศาสตร์เชิงสัญลักษณ์, ไม่จำเป็นใน app.py
# # scipy==1.16.0 # สำหรับการคำนวณทางวิทยาศาสตร์ขั้นสูง, ลองตัดออกก่อน ถ้ามีปัญหาค่อยเพิ่ม
# # tqdm==4.67.1 # สำหรับ Progress Bar, ไม่จำเป็นใน Production
